import React from "react";
import LocationControl from "./LocationControl";

const DayButton = (props) => {
  return (<button onClick={this.handleClick}>{props.day}<br />{props.location}</button>
    
  );
}

export default DayButton;